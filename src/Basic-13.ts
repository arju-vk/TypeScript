enum Direction {
  Up = 1,
  Down,
  Left,
  Right,
}

let dir: Direction = Direction.Right;

// String enum

enum Stat {
  Pending = "PENDING",
  Approved = "APPROVED",
  Rejected = "REJECTED",
}

let St: Stat = Stat.Approved;
